# 配置文件说明

## 重要提示

**`application.properties` 文件已被添加到 `.gitignore` 中,不会被提交到 Git 仓库,以保护敏感信息。**

## 首次使用步骤

1. **复制配置模板**
   ```bash
   cd SYZ/src/main/resources
   cp application-template.properties application.properties
   ```

2. **修改配置信息**
   
   编辑 `application.properties` 文件,填入你的真实配置:

   - **数据库配置**
     ```properties
     spring.datasource.url=jdbc:mysql://你的数据库地址:3306/shuangyinzi?...
     spring.datasource.username=你的数据库用户名
     spring.datasource.password=你的数据库密码
     ```

   - **Redis配置**
     ```properties
     spring.data.redis.host=你的Redis地址
     spring.data.redis.port=6379
     spring.data.redis.password=你的Redis密码
     ```

   - **阿里云短信配置** (如果使用短信功能)
     ```properties
     aliyun.sms.access-key-id=你的AccessKeyId
     aliyun.sms.access-key-secret=你的AccessKeySecret
     aliyun.sms.sign-name=你的短信签名
     aliyun.sms.template-code=你的短信模板代码
     aliyun.sms.dev-mode=false
     ```

   - **前端地址配置**
     ```properties
     app.frontend.url=http://你的前端地址:8081
     ```

## 注意事项

- ⚠️ **永远不要将 `application.properties` 文件提交到 Git 仓库**
- ✅ 只有 `application-template.properties` 模板文件会被提交
- 🔒 所有敏感信息(密钥、密码等)只保存在本地的 `application.properties` 中
- 📋 如果需要更新配置模板,请编辑 `application-template.properties` 文件

## 团队协作

新成员加入项目时:
1. 拉取代码后,按照上述步骤创建自己的 `application.properties`
2. 向项目管理员获取必要的密钥和配置信息
3. 填入配置文件后即可运行项目
